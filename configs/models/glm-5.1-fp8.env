# zai-org/GLM-5.1-FP8 — рекомендованный вариант vLLM для 8×H200/подобное (см. GLM/GLM5.md):
# https://github.com/vllm-project/recipes/blob/main/GLM/GLM5.md
# Образ vLLM: [Hub](https://hub.docker.com/r/vllm/vllm-openai/tags) — **glm51** + **cu130** (тот же семейный тег, что и bf16; не тянуть `latest`).
# Скорость: MTP (рецепт 8×H200) + выше max-num-batched-tokens. При OOM: снизить SLGPU_MAX_NUM_BATCHED_TOKENS (16384 → 12288) или убрать SLGPU_VLLM_SPECULATIVE_CONFIG.

MODEL_ID=zai-org/GLM-5.1-FP8
VLLM_DOCKER_IMAGE=vllm/vllm-openai:glm51-x86_64-cu130
MODEL_REVISION=

# JSON для vLLM --speculative-config: MTP, как в GLM/GLM5.md («Serving FP8 Model on 8xH200»).
SLGPU_VLLM_SPECULATIVE_CONFIG='{"method":"mtp","num_speculative_tokens":3}'

# Ниже полного 202k старта; приёмка по nvidia-smi — поднять как у bf16.
MAX_MODEL_LEN=200704

KV_CACHE_DTYPE=auto
GPU_MEM_UTIL=0.88
SGLANG_MEM_FRACTION_STATIC=0.90
# Chunked prefill: больше — выше throughput при нагрузке; при OOM понижать.
SLGPU_MAX_NUM_BATCHED_TOKENS=16384
SLGPU_ENABLE_PREFIX_CACHING=0
TP=8
REASONING_PARSER=glm45
TOOL_CALL_PARSER=glm47
CHAT_TEMPLATE_CONTENT_FORMAT=string
BENCH_MODEL_NAME=
